{
  "name": "everkinetic-db-scraper",
  "version": "0.0.1",
  "main": "./dist/app.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/albinotonnina/everkinetic-parser"
  },
  "engines": {
    "node": ">=0.10.8"
  },
  "scripts": {
    "start": "node dist/app.js",
    "prestart": "babel src -s -D -d dist --presets es2015,stage-0",
    "test": "mocha --require babel-core/register --recursive"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-0": "^6.16.0",
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "mockery": "^2.0.0"
  },
  "dependencies": {
    "cheerio": "^0.22.0",
    "request": "^2.79.0",
    "request-promise": "^4.1.1"
  }
}
